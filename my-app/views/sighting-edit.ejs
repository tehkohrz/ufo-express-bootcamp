<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="styles.css">
  <title>Sighting <%=sightingEntry.indexNo%> Edit</title>
</head>
<body>
  <div class='title'>Edit Sighting <%=sightingEntry.indexNo%></div>
  <form action='/sighting/<%=sightingEntry.indexNo%> >/?_method=PUT' method='POST'>
    <% for (let i=0; i < sightingEntry.keys.length; i+=1) { %>
    <label class  = 'infoLabel' for ='<%=sightingEntry.keys[i]%>'><%= sightingEntry.keys[i].toUpperCase().replace('_', ' ') %>:</label>
    <input class = 'infoInput' type = 'text' id = '<%=sightingEntry.keys[i]%>' value = '<%=sightingEntry[sightingEntry.keys[i]]%>' />
    <% } %>
    <input type='submit' value = 'Submit' />
  </form>
</body>
</html>